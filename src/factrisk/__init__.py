"""FactRisk-ST: reference-free number-preservation risk estimation for
selective speech translation.

Subpackages group modules by responsibility: ``core`` (shared primitives),
``backends`` (ST model adapters), ``datasets``, ``method`` (risk heads),
``eval`` (metrics and gates), ``pipeline`` (runnable stages), ``french``
(extension subproject), ``paper`` (manuscript assets) and ``cli``.
"""

__version__ = "0.1.0"

